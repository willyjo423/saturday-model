This folder is what GitHub Pages serves.

- `index.html`      the live dashboard, rewritten by the daily workflow
- `predictions.json` the same slate as data
- `archive/`         one JSON file per day, kept for later review
- `preview.html`     a design preview built from SIMULATED games, not real
                     predictions. Safe to delete.
